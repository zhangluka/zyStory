schema: spec-driven

# 项目上下文（可选）
# 创建制品时会展示给 AI。
# 可填写技术栈、约定、风格指南、领域知识等。
# 示例：
#   context: |
#     技术栈：TypeScript, React, Node.js
#     使用 conventional commits
#     领域：电商平台

# 按制品配置的规则（可选）
# 为指定制品添加自定义规则。
# 示例：
#   rules:
#     proposal:
#       - 提案控制在 500 字以内
#       - 始终包含 "Non-goals" 节
#     tasks:
#       - 将任务拆分为最多 2 小时一块
